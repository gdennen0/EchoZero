# EchoZero 2 Backend Audit — 2026-03-26

**Scope:** Full audit of `echozero/` codebase after persistence refactor + architecture standards.
**Test baseline:** 704 tests, 2.82s, all passing.

---

## CODE QUALITY

### Pattern Compliance — PASS (with 2 minor issues)

| Check | Status |
|---|---|
| All repos extend BaseRepository[T] | ✅ PASS — all 6 repos |
| All repos implement _from_row() | ✅ PASS |
| Zero conn.commit() in repos | ✅ PASS — 0 found |
| Named column access everywhere | ⚠️ PASS with exception — `schema.py:112` uses `row[0]` in `get_schema_version()` |
| All repos use _execute/_fetchone/_fetchall | ✅ PASS |

**Issue 1 — `schema.py:112`**: `get_schema_version()` uses `int(row[0])` tuple indexing. This runs before `row_factory=sqlite3.Row` is set in session creation, so it works, but it's the only tuple-indexed access in the codebase. Minor — functions fine since `_meta` is read before Row factory is configured.

**Issue 2 — Private function import**: `take.py:15` imports `_serialize_take_data` and `_deserialize_take_data` (underscore-prefixed private functions from `serialization.py`). These should be public since they're used cross-module. Rename to `serialize_take_data` / `deserialize_take_data`.

### Type Consistency — PASS

| Check | Status |
|---|---|
| All IDs are str | ✅ |
| All timestamps are datetime | ✅ |
| Frozen dataclasses everywhere | ✅ |
| Literal types for enums | ✅ (LayerType, TakeOrigin) |

### Docstrings — PASS

Every module, class, and public method has a docstring. Well done.

### Error Hierarchy — PASS

Clean hierarchy: `EchoZeroError` → `DomainError`/`PersistenceError`/`EngineError`/`ConfigurationError`. 
`EZ2Error` alias exists for architecture spec compatibility.
`ValidationError` correctly subclasses `DomainError`.

---

## SCHEMA ALIGNMENT

### DDL vs Code — PASS

| Table | DDL | Repos | Match |
|---|---|---|---|
| _meta | ✅ | schema.py functions | ✅ |
| projects (incl graph_json) | ✅ | ProjectRepository | ✅ |
| songs | ✅ | SongRepository | ✅ |
| song_versions | ✅ | SongVersionRepository | ✅ |
| layers | ✅ | LayerRepository | ✅ |
| takes | ✅ | TakeRepository | ✅ |
| song_pipeline_configs | ✅ | PipelineConfigRepository | ✅ |

All columns, FKs, CHECK constraints, and indexes are present and consistent between DDL and repository code.

`graph_json` is in the DDL. `_ensure_graph_column()` is deleted. ✅

---

## TRANSACTION CONTROL — PASS

| Check | Status |
|---|---|
| Repos never commit | ✅ |
| Session has transaction() context manager | ✅ |
| transaction() commits on success | ✅ (tested) |
| transaction() rolls back on exception | ✅ (tested) |
| session.commit() exists | ✅ (tested) |
| session.save() commits + clears dirty | ✅ (tested) |
| create_new() does explicit conn.commit() after initial project row | ✅ |

---

## THREAD SAFETY — PASS

| Check | Status |
|---|---|
| WAL mode enabled | ✅ (tested) |
| RLock on session | ✅ |
| row_factory = sqlite3.Row | ✅ (tested) |
| check_same_thread=False | ✅ |
| save() acquires lock | ✅ |
| save_graph() acquires lock | ✅ |
| load_graph() acquires lock | ✅ |
| autosave_tick acquires lock | ✅ |
| transaction() acquires lock | ✅ |
| commit() acquires lock | ✅ |

**Note:** DirtyTracker itself has no internal lock. `_dirty` and `_dirty_entity_ids` are accessed from both main thread and autosave thread. In practice this is safe because:
- `is_dirty()` reads a bool (atomic in CPython due to GIL)
- `clear()` sets bool + clears set (called under session lock in autosave_tick)
- `mark_dirty()` sets bool + adds to set (called from main thread)

The GIL prevents actual corruption, but it's not formally thread-safe. **LOW** risk — would only matter if we move off CPython.

---

## DIRTY TRACKING — PASS

| Event | Should Trigger Dirty? | Does it? |
|---|---|---|
| BlockAddedEvent | YES | ✅ YES |
| BlockRemovedEvent | YES | ✅ YES |
| ConnectionAddedEvent | YES | ✅ YES |
| ConnectionRemovedEvent | YES | ✅ YES |
| SettingsChangedEvent | YES | ✅ YES |
| BlockStateChangedEvent | NO | ✅ NO (tested!) |

---

## PIPELINE REGISTRY — PASS

| Check | Status |
|---|---|
| PipelineRegistry with register/get/list/ids | ✅ |
| @pipeline_template decorator | ✅ |
| PromotedParam with key/name/type/required/default/description | ✅ |
| PipelineTemplate.build() returns Graph | ✅ |
| PipelineTemplate.validate_bindings() | ✅ |
| onset_detection template exists | ✅ |
| Template builds valid graph (LoadAudio→DetectOnsets) | ✅ (tested) |
| Global registry singleton via get_registry() | ✅ |

---

## TEST COVERAGE

### What's well-tested:
- All CRUD operations for all 6 repos (73 persistence tests)
- Session lifecycle: create, open, save, close, reopen (61 session tests)
- Transaction commit + rollback
- WAL mode, row_factory
- Dirty tracker: all 5 mutation events + BlockStateChanged exclusion
- Pipeline registry: all operations + edge cases (36 tests)
- FK cascade deletes: project→song→version→layer→take chain
- Autosave: commits dirty, skips clean, stops correctly
- Crash recovery: detect, recover, discard

### Gaps Found:

**HIGH:**
1. **No test for `PipelineConfigRepository`** — CRUD tests exist in test_persistence.py for all other repos but pipeline_config is not tested there. It IS tested indirectly via test_pipelines.py but deserves its own CRUD section.

2. **No concurrent thread test** — autosave + main thread writing simultaneously. The locks are there but never stress-tested. Would catch lock ordering issues.

3. **`get_schema_version` uses `row[0]`** — should use `row['value']` or accept that it runs before row_factory is set.

**MEDIUM:**
4. **Repo properties create new instances each access** — `session.songs` creates a new `SongRepository(self.db)` every call. If repos ever gained state, this breaks silently. Consider caching.

5. **`init_db` calls `conn.executescript()` which auto-commits** — this means the initial schema creation commits outside the session's control. Acceptable for initialization but should be documented.

6. **No test for migration system** — the infrastructure exists (`_MIGRATIONS` dict, `apply_migrations`) but there are zero migrations and zero migration tests. When the first migration lands, tests need to cover: empty DB → current version, and previous version → current version.

**LOW:**
7. **Private function imports** — `_serialize_take_data`/`_deserialize_take_data` should be public.

8. **`ProjectSession.open()` stub** — currently just delegates to `open_db()` with path hashing. Will need real .ez archive support in Phase 3.

---

## ARCHITECTURE ALIGNMENT

### Panel Decisions vs Code:

| Decision | Status |
|---|---|
| Kill Branch entity | ✅ ALIGNED — never built |
| 6 entities (Project, Song, SongVersion, LayerRecord, Take, Event) | ✅ ALIGNED — all exist |
| SongPipelineConfig (ActionSet replacement) | ✅ ALIGNED — entity + repo + table |
| layer_type enum from day one | ✅ ALIGNED — CHECK constraint in DDL |
| Events as JSON blobs in Takes | ✅ ALIGNED — data_json column |
| BaseRepository[T] generic | ✅ ALIGNED |
| Unit of Work — session owns commits | ✅ ALIGNED |
| WAL + RLock | ✅ ALIGNED |
| Schema as single source of truth | ✅ ALIGNED — graph_json in DDL |
| Dirty tracking: structural only | ✅ ALIGNED |
| Pipeline templates as Python builders | ✅ ALIGNED |
| @pipeline_template decorator + registry | ✅ ALIGNED |
| PromotedParam + validate_bindings | ✅ ALIGNED |
| TakeLayer → eventually kill | ⚠️ PENDING — TakeLayer still exists in takes.py, planned for future removal |

### Not Yet Built (correctly deferred):

| Feature | Phase |
|---|---|
| .ez archive (pack/unpack) | Phase 3 |
| Audio import (content-addressed) | Phase 3 |
| AnalysisService | Phase 4 (services) |
| SetlistProcessor | Phase 4 (services) |
| More pipeline templates | Phase 4+ |
| API server (HTTP/WS) | Phase 5 |
| UI | Not started |

### No Contradictions Found

The code matches the panel decisions. Nothing blocks building the next phases.

---

## SUMMARY

| Area | Verdict |
|---|---|
| Pattern compliance | ✅ PASS |
| Schema alignment | ✅ PASS |
| Transaction control | ✅ PASS |
| Thread safety | ✅ PASS |
| Dirty tracking | ✅ PASS |
| Pipeline registry | ✅ PASS |
| Test coverage | ✅ PASS (with gaps noted) |
| Architecture alignment | ✅ PASS |

**Issues to fix (3):**
1. Make `_serialize_take_data`/`_deserialize_take_data` public (rename, remove underscore)
2. Add PipelineConfigRepository CRUD tests
3. Add concurrent thread stress test

**Issues to document (2):**
4. `get_schema_version` uses `row[0]` (acceptable — runs pre-row_factory)
5. `init_db` auto-commits via `executescript` (acceptable for initialization)

**Overall:** The backend is clean, consistent, and well-aligned with the architectural decisions. The lego patterns are working — every repo follows the same shape, every entity follows the same frozen dataclass pattern, transaction control is centralized, dirty tracking is semantically correct. Ready for Phase 3 or service layer work.
